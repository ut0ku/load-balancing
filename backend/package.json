{
  "name": "backend-servers",
  "version": "1.0.0",
  "description": "Simple backend servers for load balancing demonstration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:server1": "PORT=3000 node server.js",
    "start:server2": "PORT=3001 node server.js",
    "start:server3": "PORT=3002 node server.js",
    "start:all": "concurrently \"npm run start:server1\" \"npm run start:server2\" \"npm run start:server3\"",
    "start:all-windows": "start \"Server 1\" cmd /k npm run start:server1 && start \"Server 2\" cmd /k npm run start:server2 && start \"Server 3\" cmd /k npm run start:server3"
  },
  "keywords": [
    "load-balancing",
    "nginx",
    "haproxy"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
